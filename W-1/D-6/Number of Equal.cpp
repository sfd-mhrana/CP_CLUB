#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define endl "\n"

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    ll n, m;
    cin >> n >> m;
    ll arr1[n], arr2[m];
    for (ll i = 0; i < n; i++)
    {
        cin >> arr1[i];
    }

    return 0;
};